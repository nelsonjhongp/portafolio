---
import "./home.css";
import { asset } from "../utils/assets";
import AboutSection from "../components/about/AboutSection.astro";
import ContactPanel from "../components/contact/ContactPanel.astro";
import EducationSection from "../components/education/EducationSection.astro";
import Hero from "../components/hero/Hero.astro";
import KnowledgeSection from "../components/knowledge/KnowledgeSection.astro";
import ProjectCard from "../components/projects/ProjectCard.astro";
import ActionLink from "../components/shared/ActionLink.astro";
import BackToTop from "../components/shared/BackToTop.astro";
import Footer from "../components/shared/Footer.astro";
import SiteNavigation from "../components/shared/SiteNavigation.astro";
import SectionHeader from "../components/shared/SectionHeader.astro";
import { profile } from "../content/profile";
import { projects } from "../content/projects";
import MainLayout from "../layouts/MainLayout.astro";

const featuredProjects = projects.filter((project) => project.featured);
---

<MainLayout
  title="Nelson Jhon Gil Pari | Backend / Full-stack Junior"
  description="Portfolio de Nelson Jhon Gil Pari, estudiante de Ingeniería de Software enfocado en backend, sistemas internos, datos, inventario y herramientas administrativas."
>
  <div class="page-shell">
    <SiteNavigation
      brandHref="#top"
      brandImage="/brand/nj-logo.png"
      brandAlt="Logo de Nelson Nejigiku"
      links={[
        { href: "#projects", label: "Proyectos" },
        { href: "#stack", label: "Habilidades" },
        { href: "#about", label: "Sobre mí" },
        { href: "#education", label: "Formación académica" },
        { href: "#contact", label: "Contacto" }
      ]}
    />

    <main>
      <Hero profile={profile} />

      <section class="section section-projects section-muted" id="projects">
        <div class="section-inner">
          <SectionHeader
            fluid={true}
            title={profile.sectionTexts.projectsTitle}
            icon={asset("/icons/lucide--code-xml.svg")}
            variant="framed-left"
            text={profile.sectionTexts.projectsCopy}
          />

          <div class="projects-grid">
            {featuredProjects.map((project) => <ProjectCard project={project} />)}
          </div>

          <div class="projects-actions" aria-label="Más acciones de proyectos">
            {profile.projectCtas.map((cta) => (
              <ActionLink
                className="projects-action-link"
                href={cta.href}
                label={cta.label}
                kind={cta.kind}
                pending={cta.pending}
                title={cta.title}
                hideIcon={true}
              />
            ))}
          </div>
        </div>
      </section>

      <KnowledgeSection profile={profile} />
      <AboutSection profile={profile} />
      <EducationSection profile={profile} />
      <ContactPanel profile={profile} />
    </main>

    <Footer name={profile.name} />
    <BackToTop />
  </div>
</MainLayout>
